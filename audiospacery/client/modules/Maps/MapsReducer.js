// Import Actions
import {  } from './MapsActions';

// Initial State
const initialState = {};

const MapsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default MapsReducer;
