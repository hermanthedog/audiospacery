// Import Actions
import {  } from './MenuActions';

// Initial State
const initialState = {};

const MenuReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default MenuReducer;
