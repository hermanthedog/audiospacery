// Import Actions
import {  } from './WrapperActions';

// Initial State
const initialState = {};

const WrapperReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default WrapperReducer;
